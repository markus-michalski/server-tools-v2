# =============================================================================
# Server Tools Configuration
# =============================================================================
# Copy this file to /etc/server-tools/config and adjust values.
# All settings have sensible defaults - only uncomment what you need to change.
# =============================================================================

# --- Paths ---

# Directory for database credential files
#ST_CREDENTIAL_DIR="/root/db-credentials"

# Audit log file location
#ST_AUDIT_LOG="/var/log/server-tools-audit.log"

# Backup directory for pre-deletion backups
#ST_BACKUP_DIR="/root/server-tools-backups"

# Database backup directory (for dump/export operations)
#ST_DB_BACKUP_DIR="/root/db-backups"

# --- PHP & Apache ---

# Default PHP version for new virtual hosts
#ST_DEFAULT_PHP_VERSION="8.3"

# PHP versions to scan for FPM sockets
#ST_PHP_VERSIONS_TO_SCAN="7.4 8.0 8.1 8.2 8.3 8.4"

# Apache ServerAdmin email
#ST_APACHE_SERVER_ADMIN="webmaster@localhost"

# Allowed base paths for DocumentRoot (colon-separated)
#ST_ALLOWED_DOCROOT_PATHS="/var/www:/srv/www"

# --- SSL ---

# Default email for Let's Encrypt certificates
#ST_CERTBOT_EMAIL="admin@example.com"

# DNS provider for wildcard SSL (cloudflare, digitalocean, route53, google, linode, ovh)
#ST_DNS_PROVIDER=""

# Path to DNS API credentials file (auto-detected if empty)
#ST_DNS_CREDENTIALS_FILE=""

# --- MySQL ---

# MySQL config file with root credentials
#ST_MYSQL_CONFIG_FILE="/root/.my.cnf"

# Default character set for new databases
#ST_DEFAULT_CHARSET="utf8mb4"

# Default collation for new databases
#ST_DEFAULT_COLLATION="utf8mb4_unicode_ci"

# --- Passwords ---

# Default generated password length
#ST_PASSWORD_LENGTH=25

# Minimum accepted password length
#ST_PASSWORD_MIN_LENGTH=12

# --- Features ---

# Automatically backup configs before deletion
#ST_AUTO_BACKUP=true

# Backup retention in days (cleanup removes older backups)
#ST_BACKUP_RETENTION_DAYS=30

# Enable audit logging
#ST_AUDIT_LOGGING=true

# --- Logs ---

# Apache log directory
#ST_APACHE_LOG_DIR="/var/log/apache2"

# MySQL error log file
#ST_MYSQL_LOG_FILE="/var/log/mysql/error.log"

# Default number of log lines to display
#ST_LOG_LINES=50

# --- Credential Files ---

# File permissions for credential files (octal)
#ST_CREDENTIAL_FILE_PERMISSIONS=600
